# Roadmap / Backlog — доделки и технический долг

Документ производный от [TASK_STATE.md](TASK_STATE.md), [KNOWN_ISSUES.md](KNOWN_ISSUES.md) и
[CHECKLIST.md](CHECKLIST.md) на версии **0.18.0**. Базовый функционал спецификации
(чеклист А–Р) закрыт; ниже — снятие ограничений «первых версий», подтверждение
сквозного результата на проде и технический долг.

Каждая карточка ссылается на конкретный открытый пункт `KNOWN_ISSUES` (KI #N) или
⚠️ пункт `CHECKLIST`. Порядок волн: дешёвое и разблокирующее → готовый бэкенд в UI →
содержательные доработки → подтверждение на проде → QA-инфраструктура.

Статусы: ⬜ не начато · 🔄 в работе · ✅ сделано.

---

## Волна 0 — Технический долг навигации и настроек

Дёшево, без внешних зависимостей. Делается первой.

### 0.1 Навигация и мёртвый код — ✅ (KI #35, 2026-06-26)
Вернуть пункт «Воронки» (owner/admin) в живое меню `frontend/src/layout/AppMenu.vue`,
либо встроить управление воронками в раздел «Сделки»/«Настройки». Удалить
неиспользуемые `frontend/src/layouts/SidebarNav.vue` и `frontend/src/layouts/TopBar.vue`.
Обновить `docs/user-guide/15-pipelines.md`, убрав описание обходного пути через прямой URL.
**Валидация:** typecheck + build + ручной переход на `/app/pipelines` из меню.

### 0.2 Порядок в SettingsView — 🔄 частично (2026-06-26)
- **KI #21 — ✅** Таб «Чаты» и его чат-логика удалены из
  `frontend/src/views/ChannelsView.vue`; редирект `?tab=chats` → `/app/chats`.
- **KI #19 — ✅ закрыто решением** Роль «Администратор» по решению владельца
  намеренно сохраняет право менять настройки организации; защита не вводится.
- **KI #20 — ⬜ отложено** Косметический скачок отступов вложенных вкладок Настроек
  оставлен в бэклоге (вынести «settings-panel» без `<section>`-обёртки либо передать
  встраиваемым вкладкам признак `embedded`, снимающий внешние отступы).

---

## Волна 1 — Достать готовый бэкенд в UI

Серверная логика реализована и покрыта тестами; недоделан только фронтенд.

### 1.1 Теги в карточках и фильтрах — 🔄 частично (2026-06-26, KI #30)
- **✅ Сделано** Чипы тегов и `PMultiSelect` назначения добавлены в карточки
  контакта (`ContactDrawer.vue`) и сделки (`DealDetailView.vue`); сериализаторы
  `get_contact`/`get_deal`/`kanban_deals` возвращают `tags`. Фильтр «Тег» выведен
  в тулбар контактов (серверный `tag_id`) и в фильтры канбана сделок (клиентский).
  Валидация: backend `test_tags` 7/7, регрессия `apps.crm` 57/57, ruff чисто,
  typecheck + build + vitest 11/11 + e2e 9/9.
- **✅ Сделано (2026-06-26, Волна 2)** UI сегментов: выпадающий список сегментов и
  кнопка «сохранить фильтр как сегмент» в списках контактов и сделок поверх CRUD
  `segments/` (`listSegments`/`createSegment`). KI #30 закрыт полностью.

### 1.2 Действия автоматизаций в конструкторе — ✅ (KI #31, 2026-06-26)
Действия `change_stage`/`assign`/`create_document` выведены в
`frontend/src/views/AutomationView.vue` (выбор стадии/менеджера/шаблона);
добавлены условия «Воронка»/«Стадия»; в `automation_api._validate` добавлена
защита обязательных параметров действий. Остался триггер `sla_breach` — он
вынесен в отдельную задачу 2.5 (Волна 2). Валидация: backend `test_automation`
10/10, ruff чисто, typecheck + build + vitest 11/11 + e2e 9/9.

**Волна 1 завершена** (задача 1.1 — теги, кроме сегментов; задача 1.2 — автоматизации).

---

## Волна 2 — Содержательные доработки «первых версий»

### 2.1 Email-канал: вложения и тред-заголовки — 🔄 частично (2026-06-26, KI #28)
- **✅ Сделано** `parse_email` сохраняет содержимое вложений (base64, лимит 5 МБ) +
  захватывает HTML и `In-Reply-To`/`References`; `ChatsTab` даёт ссылку скачивания;
  исходящий ответ проставляет тред-заголовки из `Message-ID` последнего входящего.
  Тесты `EmailParseAndThreadingTest`. Backend `channels` 127-регрессия зелёная.
- **⬜ Осталось** Безопасный рендер инлайн-HTML (нужно поле модели + санитайзер,
  XSS-риск) и OAuth Gmail/Yandex.

### 2.2 Веб-формы: типы полей и капча — 🔄 частично (2026-06-26, KI #29)
- **✅ Сделано** Типы полей `select`/`checkbox` в конструкторе и виджете; подключаемая
  серверная капча (`captcha.py`, no-op без `CAPTCHA_SECRET`, контракт siteverify),
  site_key в схеме, рендер reCAPTCHA/hCaptcha в виджете. Тесты `CaptchaServiceTest`.
- **⬜ Осталось** Тип «файл»; боевая проверка виджета и капчи на внешнем сайте
  (нужны боевые ключи — сквозным результатом из dev не проверяется).

### 2.3 Импорт/экспорт сделок — ✅ (KI #32, 2026-06-26)
Импорт и экспорт сделок добавлены: разрешение FK по имени (воронка/стадия/контакт),
дедуп по `название+контакт`, экспорт `export_deals_csv`, переключатель «Сделки» в
`DataToolsView`. Слияние сделок осознанно вне охвата (транзакционные записи).
Тесты `DealImportExportServiceTest`, e2e 9/9. Остаток KI #32 (загрузка крупных
файлов через стораджи; UI слияния контактов/компаний с предпросмотром) не закрывался.

### 2.4 Воронка на истории + бэкафилл closed_at + планы по воронкам — ✅ (KI #34, 2026-06-26)
- **✅ Бэкафилл `closed_at`** — сервис `backfill_closed_at()` + миграция `crm/0013`.
- **✅ Честная воронка по истории** — модель `StageTransition` (лог переходов в
  `create_deal`/`move_deal`), `funnel` отдаёт `reached`/конверсию/`history_since`,
  таблица «Прохождение по стадиям» в `StatsView`. Тесты `HonestFunnelTest`.
- **✅ Планы по воронкам и командные цели** — `SalesTarget` получил `pipeline`
  (NULL = все) и командную цель (`responsible` NULL); `target-progress` считает
  факт в области плана; `SalesTargetsView` даёт выбор воронки и «Команда».
  Миграция `crm/0015`. Тесты `TargetScopeTest`. **KI #34 закрыт полностью.**

**Волна 2 завершена** (2.1–2.7 + сегменты + KI #34).

### 2.5 Триггер sla_breach — ✅ (KI #31, 2026-06-26)
Реализован в beat `evaluate_time_rules` по образцу `no_activity` (метрика —
время на текущей стадии по последней `stage_change`, идемпотентность через
`AutomationRunLog`); добавлен в `_TRIGGERS` и в конструктор `AutomationView.vue`
с полем «Дней на стадии (SLA)». Тесты `test_sla_breach_*`. KI #31 закрыт полностью.

### 2.6 Командный календарь — 🔄 частично (2026-06-26, KI #33)
- **✅ Сделано** Scope `mine|team` в `calendar_activities` (для владельца/админа —
  все задачи с подписью ответственного), переключатель «Мои/Все задачи» и drag-перенос
  срока (`eventDrop` → `patch_activity`). Тесты `CalendarScopeTest`.
- **⬜ Осталось** Визуальный конструктор сложных RRULE и code-splitting тяжёлых вью.

### 2.7 ВКонтакте — снятие ограничений — 🔄 частично (2026-06-26, KI #22)
- **✅ Сделано** Реальное имя контакта через `users.get` (`get_vk_user_name`) при первом
  входящем сообщении. Тесты с моком; сквозной результат с боевым токеном не проверялся.
- **⬜ Осталось** Исходящие вложения, отрисовка стикеров/опросов/геолокации, лид-формы
  и комментарии под постами.

---

## Волна 3 — Подтверждение сквозного результата на проде

Не кодинг, а деплой и QA с чтением логов.

### 3.1 Боевая проверка телефонии Exolve — ⬜ (KI #23, #24, #25)
Нужны боевой `EXOLVE_API_KEY`, номер, `EXOLVE_PUBLIC_BASE_URL`, `EXOLVE_WEBHOOK_SECRET`.
Проверить автозакупку номера, провижининг SIP, регистрацию софтфона, входящий
(сделка + дедуп + дозвон), исходящий, журнал и запись. Подтвердить `GetFree`,
`REDIRECT_NUMBER`, автопроигрывание аудио в Web Voice SDK. Самодиагностика:
`apps/telephony/exolve_client.py`, `apps/telephony/public_views.py`.

### 3.2 Сквозная транскрипция и AI-резюме — ⬜ (KI #36)
После деплоя 0.18.0 прогнать реальную запись по пути «Deepgram → Hermes → активность
в таймлайне» (логгер `apps.telephony.transcription`). Доработки: тарифный лимит минут
транскрипции, диаризация спикеров. Зависит от 3.1.

### 3.3 ~~Боевая валидация внешних CRM~~ — ❌ ВНЕ ОБЛАСТИ (решение владельца)
Интеграция со сторонними CRM (amoCRM/Битрикс24 и любыми другими) проекту не нужна —
продукт строится только на встроенном CRM. Задача удалена из плана. Существующий код
`apps/integrations/` — легаси/дремлющий; удаление или сохранение решается отдельно.
См. KNOWN_ISSUES #1 и память `feedback_no_external_crm`.

### 3.4 Сквозная проверка ЮKassa — ⬜ (CHECKLIST #71, #73)
С боевыми credentials подтвердить создание платежа с редиректом и активацию подписки
через webhook.

---

## Волна 4 — Тестовая и фронтенд-инфраструктура

### 4.1 Расширить e2e-покрытие — ⬜ (KI #3)
Playwright-сценарии на tenant switch, документы/подписание, чаты/мессенджеры, телефонию.

### 4.2 Нагрузочный профиль — ⬜ (KI #3)
Performance-профиль для API, WebSocket, Celery.

### 4.3 Декомпозиция фронтенда — ⬜ (KI #15)
Разбить `frontend/src/views/TelephonyView.vue` и `frontend/src/views/DocumentsView.vue`
(~555 строк) по паттерну DEC-036 с гейтом typecheck + vite build.

### 4.4 Браузерный QA мобильной адаптации — ⬜ (KI #17)
Прогон на эмуляторе и реальном телефоне для ширин 320/375/414/768/1024px,
результат в DEV_LOG. Требует браузера (нет в dev-среде).
