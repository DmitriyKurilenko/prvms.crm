# Task State

## Текущие задачи
| # | Задача | Статус | Заметки |
|---|--------|--------|---------|
| 1 | Frontend SPA (Vue 3 + PrimeVue) | done | Инициализировано приложение, layout, router guards, stores, страницы, компонентный слой, dev/prod Docker. Layout перестроен на официальный Sakai шаблон (fixed topbar + sidebar, useLayout composable) |
| 2 | Аудит + уведомления | done | API, UI-страницы, realtime уведомления через WebSocket; чеклист Л (55-58) ✅: Telegram bot webhook, async email, матрица настроек event×канал, Telegram-привязка в UI |
| 3 | CRM-интеграции + Встроенный CRM | done | Реализованы OAuth/webhook API, health/sync задачи, ротация секретов; адаптеры amoCRM/Bitrix24/Builtin полностью реализованы с реальными HTTP-вызовами; frontend: Kanban, контакты/компании/воронки CRUD |
| 4 | Распределение заявок | done | Триггер `new_deal`; стратегии min_load/round_robin/weighted/manual_queue; авто-синхронизация менеджеров из команды; авто-распределение при создании сделки; лог с названием сделки; frontend: PMultiSelect менеджеров, PSelect fallback; чеклист Е (30-33) ✅ |
| 5 | Договоры и подписание | done | Системные шаблоны; WYSIWYG-редактор шаблонов; auto-mapping; signing flow: CRM формирует ссылку → клиент открывает → запрашивает OTP → вводит код → подписано; ПЭП (SHA-256 + HMAC-SHA256); PDF download fix; автозаполнение телефона; скачивание/email после подписания; блок ПЭП в PDF (полный телефон, IP, UUID); чекбокс согласия на ЭП + ссылка на соглашение; автоподписание соглашения об ЭП при первом подписании; статус ЭДО + скачивание соглашения в профиле контакта |
| 6 | Мессенджер-каналы | done | CRUD каналов с credentials по типу (Telegram/WhatsApp/MAX); авто-регистрация webhook для Telegram и MAX (Bot API platform-api.max.ru); webhook URL; auto_create_lead; чаты с перепиской и отправкой ответа; incoming/outgoing routing через Celery; real-time WS (DEC-019); чеклист З (38-41) ✅ |
| 7 | Телефония (FreeSWITCH) | done | dialplan/events, trunk/ivr/queue API, CDR, генерация FS-конфигов, WebRTC credentials; ESL: originate/check_sip_registrations через greenswitch; frontend: 6 вкладок + SIP.js софтфон; чеклист И (42–51) ✅: фильтры журнала, HTML5-плеер + скачивание записей, click-to-call, IVR-конструктор digit→action |
| 8 | Дашборд и аналитика | done | Готовы агрегирующие API и UI, включая менеджерские/канальные/телефонные KPI и фильтрацию по периоду |
| 9 | Пробный период + модуль оплаты | done | 7-дневный триал при регистрации, выбор плана, Payment с интеграцией ЮKassa (создание платежа, redirect на оплату, webhook подтверждение), frontend: баннер триала, страница подписки, redirect на ЮKassa |
| 10 | Команда и менеджеры (чеклист K) + multi-org tenant switch | done | Приглашения: pending/accept/resend/cancel, безопасное принятие для существующего аккаунта с подтверждением пароля, список организаций пользователя и переключение текущей организации в SPA |
| 11 | Hotfix: onboarding invites + legacy empty-password accept | done | Шаг 3 онбординга теперь формирует реальные pending приглашения в организацию; legacy-пользователи с пустым паролем корректно проходят через установку нового пароля на `/invite/accept` |
| 12 | RBAC для CRM (чеклист Р) | done | Добавлена матрица прав ролей (entity × action + scope all/team/own), enforcement в API и UI, аудит изменений прав, backend/frontend регрессии |
| 13 | Этап Ж: внешние CRM (webhook + marketplace UX) | done | Закрыты 34–37 + 85–94: marketplace start amo/bitrix, dual-flow setup, автонастройка после callback (webhook + sync + health), test/reconnect/scopes/statuses/error-log API, обновлённый UI «Интеграции», e2e регрессии |
| 14 | Раздел «Помощь» (user-guide) | done | 18 статей пользовательской документации в `docs/user-guide/*.md`, новый пункт меню «Помощь» в SPA, страница `HelpView` с поиском/TOC, свой md-рендер без зависимостей, symlink `frontend/src/docs/user-guide` → `docs/user-guide` |
| 15 | Аудит (чеклист М, пп. 59–61) | done | Фильтры по пользователю/действию/дате с серверной пагинацией; diff-таблица «Поле / Было / Стало»; экспорт CSV с полным набором колонок и поддержкой тех же фильтров |
| 16 | Подписка и тарифы hardening (чеклист Н, пп. 62–64) | done | Разрешён subscription/billing контур при `trial_expired`, usage унифицирован и расширен (`pipelines`), managers считаются по Membership, feature-gating выровнен в API/UI, DTO планов унифицирован между `/billing/plans/` и `/tenant/plans/` |
| 17 | Дашборд KPI (В, пп. 9–11) + Настройки организации (О, п. 65) | done | Presence через WebSocket consumer + Redis TTL; `/dashboard/managers-online/` endpoint; DashboardView: KPI менеджеры онлайн + polling; SettingsView: загрузка логотипа, timezone/language/brand_color; логотип в сайдбаре; CSS var `--brand`; datetime util с tenant timezone/locale; Accept-Language header |
| 18 | Redesign PR1 — дизайн-система + тёмная тема | done | PrimeVue Aura preset переопределён под indigo primary; в `main.css` добавлены токены редизайна (`--primary-*`, `--surface-*`, статусные `-50/-500` палитры, `--radius/--shadow`); шрифт Nunito Sans через `<link>` в index.html; `stores/ui.ts` стал источником истины для темы с persistence в `localStorage['crm.theme']` + fallback на prefers-color-scheme; `useLayout` делегирует тему в ui store |
| 19 | DevOps: bootstrap скрипт первичной настройки сервера | done | Добавлен `for_sample_deploy/bootstrap-server.sh`: установка Docker/Compose (Debian/Ubuntu), подготовка `/opt/traefik` + `/opt/portainer` + `/opt/scripts`, гарантированное создание сети `proxy`, генерация `start-all.sh/stop-all.sh/status-all.sh`, запуск Traefik + Portainer без конфликта restart policy и без ошибки `network proxy not found` |
| 20 | UI Error Handling Hardening (try/catch + Toast + planLoaded guards) | done | 21 файл: catch + toast в 11 views/components, SameSite cookie fix (backend), защита от сброса access-токена при ошибке refresh, исправлен ws.onclose, план-зависимые блокировки защищены `planLoaded`, `<PToast/>` в App.vue, `appType: 'spa'` в vite.config.ts |
| 21 | Functional hardening: messengers, session, deals, distribution | done | 8 файлов: Pipeline seeding в registration/onboarding-skip, auto_create_lead try/except + is_active filter, cookie samesite Lax(dev)/None(prod), quick-create contact/company в DealsView, distribution trigger new_lead→new_deal + synonym fallback, SOURCE_CHOICES + builtin_crm |
| 22 | Полный рефакторинг A-E (DEC-032) | done | Закрыты KNOWN_ISSUES #4-#6: ai_assistant tenant FK + поле hermes_conversation_id (миграция перегенерирована); Vite working_dir → /srv/app; npm typecheck зелёный (12 `as any` устранены, типы CrmContact/CrmDeal/IvrMenu расширены, http.ts retry вынесен в обёртку). Архитектура: новый apps/tenants/services.py с provision_tenant(); apps/users/api.py 769 LOC → shim 18 LOC + auth_api/team_api/managers_api; bare except Exception сужены до TokenError/DoesNotExist/RequestException/OSError. Frontend: CRMView.vue 2023 LOC удалён; новые CompaniesView/PipelinesView/StatsView (~760 LOC); structured logger в stores/views; sidebar расширен. 118/118 backend tests + 5/5 vitest зелёные |
| 23 | DevOps: системное исправление HTTPS на shared VPS | done | Удалён временный `fix-crm-https.sh`. `fix-https.sh`/`start-all.sh`/`check-https.sh`/`deploy.sh` теперь гарантируют: overlay attachable сеть `proxy`, перезапуск Traefik после всех проектов, проверка роутеров через Dashboard API. DEC-033 |
| 24 | Корневое исправление HTTPS: healthcheck + tenant middleware + Traefik filtering | done | `HealthCheckBypassMiddleware` отвечает `/healthz` до tenant resolution (web стал healthy). Healthcheck `frontend-app` использует IPv4-литерал `127.0.0.1` (обход busybox-wget без IPv6 fallback). `start-all.sh` имеет preflight на `PUBLIC_HOSTNAME` в crm_prvms. `.gitignore` переписан: убран блок-исключение `/vps-deployment`, добавлены прицельные паттерны для секретов. Удалён случайный снапшот `.venv.current_on_server` с production-ключами. DEC-034 |
| 25 | Исправление создания сделок из Telegram/MAX + рефакторинг мессенджер-каналов | done | DEC-035: `normalize_incoming_payload` теперь фильтрует `edited_message`/`callback_query`/`bot_started`; `_find_pipeline_and_stage()` с явным логированием; `_auto_create_lead()` записывает `message.error` при отсутствии pipeline/stage; узкие `except` в tasks.py; тесты расширены с 3 до 13. 128/128 backend tests + 5/5 vitest зелёные.
| 26 | Рефакторинг P0–P2 (DEC-036) | done | Декомпозиция по паттерну DEC-032: `crm/api.py` 864→shim (6 доменных модулей + `_api_common` + `schemas`), `integrations/api.py` 705→shim (`connections/webhooks/oauth_api`), `contracts/services.py` 558→shim (`mapping/pdf/otp/esign_agreement/signing`). Восстановлен инвариант «0 `as any`» (`CrmDeal` дополнен, SIP.js типизирован). `useApiCall` composable — единая точка DEC-031, `DealsView` полностью переведён. Поведение API/signing неизменно (shim re-export). **P2-1 выполнен** (включая ChatsTab): `DealsView` 760→623, `IntegrationsView` 645→415, `ChannelsView` 605→452 — 9 новых презентационных компонентов, состояние/WS остаются в родителях (поведение-эквивалентно; ChatsTab экспонирует `scrollToBottom()` через `defineExpose`, родитель вызывает в тех же точках управления). Валидационный гейт усилен `vite build`. Вошло в релиз `0.2.6` (2026-05-16) вместе с DEC-037. 128/128 backend + 24/24 целевые + typecheck EXIT=0 + build EXIT=0 (706 модулей) + 5/5 vitest. Будущие кандидаты: `TelephonyView`/`ContractsView` (KNOWN_ISSUES #15). |

| 27 | Адаптация UI для мобильных (DEC-037) | done | Корневая причина «меню не скрывается» — баг CSS-специфичности (`.layout-static .layout-sidebar` 0,2,0 перебивал media-правило 0,1,0), не JS. Структурный фикс: desktop-режимы → `@media (min-width:992px)`, mobile off-canvas → `@media (max-width:991px)` (взаимоисключающие диапазоны). Single-source адаптивный слой в `main.css`: глобальные `.form-grid/.form-row-2/.form-row-3` (scoped-дубли удалены из 6 файлов), `.section-header`, `.p-dialog/.p-drawer max-width:95vw`, адаптив topbar. Директива `v-responsive-table` (PrimeVue-class-agnostic, сверена с DOM PrimeVue 4.4) → карточный режим на ≤767px, применена ко всем 24 `PDataTable`. Per-view: Tasks/Assistant 1-колоночные на ≤768px, tab-bar wrap. Валидация: `manage.py check` 0 issues, typecheck EXIT=0, build EXIT=0, 5/5 vitest, маркеры в бандле + dev SPA 200. Браузер-QA на устройстве не выполнялся (нет браузера в среде) — KNOWN_ISSUES #16. |
| 28 | Сид-скрипт демо-пользователей | done | Новая management-команда `seed_demo_users`: 10 tenant-ов (`company-1`..`company-10`), 3 пользователя на каждый (owner/admin/manager) + platform admin. Формат вывода `email\password`. Пароль по умолчанию `Asdf2121`. Идемпотентный: get_or_create для tenant/user/membership. Провижионинг через `provision_tenant()` — pipeline + preferences. Покрыт тестом `test_seed_demo_users_command.py`. 129/129 backend tests OK. |
| 29 | Hotfix: авторизация в dev-режиме (cookie SameSite) | done | `_set_refresh_cookie` использовал `samesite='None'` безусловно. В dev (`secure=False`) браузеры отбрасывали refresh_token cookie → авторизация падала при refresh. Исправлено: `samesite='Lax' if DEBUG else 'None'`. 129/129 backend tests OK, typecheck EXIT=0, build EXIT=0. |
| 30 | Реструктуризация меню и навигации CRM | done | Сделка — полноценное окно с id в URL; чат внутри сделки с переключением каналов; в контакте — связанные сделки; компании в меню; мессенджеры перенесены в настройки, в меню — Чаты; интеграции неактивны; распределение внутри Команды; уведомления внутри Настроек; Помощь починена (Dockerfile + symlink). 129/129 backend tests, typecheck EXIT=0, build EXIT=0, 5/5 vitest, SPA routes 200. |
| 31 | SEO-лэндинг с PageSpeed-оптимизацией | done | Корневой `/` теперь рендерит Django-шаблон `templates/landing.html` вместо редиректа на SPA. Инлайн CSS, системные шрифты, семантический HTML5, Open Graph, Twitter Cards, JSON-LD (Organization + WebSite + SoftwareApplication + FAQPage), skip-link, focus-visible, адаптивная сетка. В production Traefik router `crm-api` теперь захватывает `Path(\`/\`)`. 129/129 backend tests, `manage.py check` 0 issues. **2026-06-18: страница переписана с нуля под бренд «ГусьБерри» по `land_spec`** — маскот-гусь инлайн-SVG, тёмная тема (prefers-color-scheme + ручной тумблер с localStorage, анти-FOUC), уровни Соло/Команда/Бизнес с калькулятором (контракт `quote/` сохранён), форма «Написать нам» на `telephony-request/`, JSON-LD без фиктивного рейтинга, ноль внешних ресурсов. Валидация: `check` 0 issues, рендер `/` 200, сквозные вызовы `quote/` и `telephony-request/` 200, honeypot 400. Визуальный QA и PageSpeed в браузере не выполнялись (нет браузера в среде). |
| 32 | Канал ВКонтакте | done | Реализован канал ВКонтакте через Standalone OAuth + Callback API (DEC-039). Модель, провайдер, webhook, OAuth API, фронтенд (VkCallbackView.vue, кнопка, иконка), тесты (33/33). Валидация: `manage.py check` 0 issues, `docker compose exec frontend npm run typecheck` EXIT=0, `docker compose exec frontend npm run build` EXIT=0, `docker compose exec frontend npm run test` 5/5. |
| 34 | Тарифы v2 — конфигуратор СВОБОДНОГО плана + калькулятор на лендинге | done | Новые тарифы СОЛО/КОМАНДА/СВОБОДНЫЙ с v2-полями (messengers, inbound_channels, signatures, telephony). Модели `PricingQuote`, `TelephonyQuoteRequest`. Публичные endpoint-ы `/api/public/pricing/quote/` и `/api/public/pricing/telephony-request/`. Интерактивный калькулятор на лендинге с переходом в регистрацию с `?plan=` и `?quote_id=`. Поддержка `free-custom` + `quote_id` в регистрации; `Tenant.custom_limits` хранит вычисленные лимиты. Миграции: `0005_plan_pricing_v2`, `0006_seed_plans_solo_komanda`, `0007_migrate_tenants_to_v2_plans`. Тесты: 10 новых в `test_pricing_calculator.py`. 49/49 backend tests OK, typecheck EXIT=0, build EXIT=0 (730 модулей), 5/5 vitest. DEC-041. |
| 33 | Настройка проекта для работы через shared Traefik reverse proxy | done | Убран nginx из `docker-compose.prod.yml`; добавлены Traefik labels к `web` и `frontend-app` с уникальными роутерами (`prvms-api`, `prvms-static`, `prvms-spa`); добавлена сеть `traefik` (external: true); explicit image tags для `web` и worker-сервисов; убраны nginx-переменные из `.env.prod.example`, добавлен `TRAEFIK_HOST`; обновлён `deploy.sh` — проверка Traefik перед деплоем, ожидание `frontend-app` вместо nginx; удалены устаревшие `for_sample_deploy/` и `vps-deployment/` (legacy deploy configs); `docker compose config` проходит. DEC-040. |
| 35 | Телефония MTS Exolve (полная замена FreeSWITCH) | done | DEC-042, версия 0.7.0. FreeSWITCH удалён целиком; облако Exolve (Numbering/SIP API + Web Voice SDK + Call Events). Один номер на тенант (автозакупка через мастер), авто-SIP менеджеров, входящий через IPCR `getControlCallFollowMe` создаёт сделку с контролем дублей и маршрутизирует ответственному→остальным, голос в браузере, кнопки звонка из контакта и сделки, журнал/запись. Валидация: `check` 0 issues, миграции без дрейфа, 131/131 backend, typecheck/build/vitest зелёные, рендер 200, webhook-и отвечают корректно. Реальный голосовой звонок требует боевого ключа Exolve и прода — KNOWN_ISSUES #23–25. |
| 36 | Рефакторинг модуля «Договоры» → «Документооборот» | done | Полный переход: `apps/contracts` → `apps/documents`, модели `Contract/ContractTemplate` → `Document/DocumentTemplate`, добавлен `DocumentType` (contract/act/invoice/offer/addendum/other), у `DocumentTemplate` поле `document_type` + системные шаблоны для каждого типа, feature codes `documents`/`document_signing`/`custom_document_templates`, лимит `max_documents_per_month`, event `document_signed`, activity type `document`, trigger `create_document`, frontend `/app/documents`, API `/api/documents/`. Без legacy и обратной совместимости; миграции пересозданы на пустой БД. Валидация: backend 63/63, frontend typecheck/build/test, ручные HTTP-проверки. |
| 37 | Сквозной рефакторинг проекта (Блоки 0–5, DEC-044) | done | Версия 0.8.1. Блок 0: удалён посторонний `node_modules` в `src/composables` (24 МБ), спецификации в `docs/specs/`, удалён легаси `nginx/`+`setup-ssl.sh`, расписание beat уведено в `/tmp`. Блок 1: `LOGGING` в settings с доменными логгерами интеграций. Блок 2: ruff (F/E/B/BLE/I) через `pyproject.toml`+`requirements-dev.txt`+CI lint-job; 146 авто-фиксов; устранён реальный латентный `NameError` (F821 в ai_assistant/consumers.py); сужен `except`→`DisallowedHost`; 6×B904; E501 отложен на `ruff format`; починен устаревший landing-тест. Блок 3: 6 крупных views декомпозированы по паттерну DEC-036 (DealChatTab/TemplateHtmlEditor/DealsKanbanBoard/ContactFormDialog/ManagerEditDialog/TriggerConfigDialog), 3233→2778 строк, удалён мёртвый KanbanBoard+DealCard. Блок 4: триплицированная логика membership → `reconcile_membership`; +6 vitest-тестов datetime (5→11). Блок 5: синхронизированы KNOWN_ISSUES #11/#15. Валидация: down/up --build, `check` 0 issues, нет дрейфа миграций, ruff чисто, 132/132 backend, typecheck/build OK, vitest 11/11, HTTP `/`200 `/healthz`200 `/app`302 `/api/docs`200. Браузер-QA 6 экранов не выполнялся (нет браузера в среде) — KNOWN_ISSUES #17. В этот же релиз вошла доводка лэндинга по Lighthouse (контраст `#43a047`→`#2e7d32`, gzip-middleware, `frontend/public/{robots.txt,sitemap.xml,llms.txt}`, инлайн-логотип WebP) — см. DEV_LOG 2026-06-18. |
| 38 | Автогенерация slug организации при регистрации (DEC-046) — **0.9.0** | done | Убрано поле «Слаг» из формы регистрации; slug генерируется CRM на основе названия организации с транслитерацией кириллицы и гарантией уникальности. Валидация: down/up --build, `check` 0 issues, ruff чисто, backend 9/9 + 13/13 целевые + 54/54 по затронутым приложениям, frontend typecheck/build/vitest зелёные, ручная POST /api/auth/register без org_slug → 201 + tenant_slug. |

| 38 | Приём формы обратной связи лендинга по почте (DEC-045) | done | Форма «Написать нам» доставляет письмо на `CONTACT_TO` через SMTP. `settings.py` маппит env хостинга (`SMTP_*`/`CONTACT_*`) на Django `EMAIL_*`+`SUPPORT_EMAIL`; nodemailer `secure`→взаимоисключающие `EMAIL_USE_SSL/TLS`. Обработчик `public_views` различает источник (`landing-contact`/телефония), формирует читаемое письмо, логирует постановку; `send_email_async` логирует `delivered=N` и ловит узкие SMTP/OSError. `.env`/`.env.example`/`.env.prod.example` приведены к паритету, dev переключён на smtp-бэкенд. Валидация: down/up --build, `manage.py check` 0 issues, ruff чисто, 134/134 backend, +2 целевых теста (мок `send_email_async.delay`). Живой зонд: POST формы→200, celery `delivered=1` (Beget принял по SSL/465). Приход в ящик подтверждает получатель — уровень «сквозь» не из среды разработки. **Hotfix 2026-06-21 (0.8.2/0.8.3):** убран молчаливый fallback на `console` при заполненном `SMTP_HOST` (автовыбор SMTP + system check `notifications.W001`); в production-стеке `celery` подключён к сети `traefik` для исходящего SMTP, `send_email_async` ретраится при `SMTPException`/`OSError`. |

| 39 | Товарный каталог + позиции сделки (P0, Фаза 1, DEC-047/DEC-048) — **0.10.0** | done | Модели `Product`/`ProductCategory`/`DealItem` (цена/НДС/наименование — снимок); сервис пересчёта `services/pricing.py` (Σ позиций → `Deal.amount`, единое округление); API `products_api`/`deal_items_api`; интеграция позиций в контекст документа (`documents/mapping.py`); сущность прав `products`; фронтенд — раздел «Товары», вкладка «Позиции» в карточке сделки (сумма блокируется при наличии позиций), метка в матрице прав. Каталог гейтован `crm_builtin` (без отдельной фичи). Валидация: миграции `crm/0006`+`users/0004` без дрейфа, `manage.py check` 0 issues, 33 целевых backend-теста OK (каталог + RBAC), typecheck/build (627 модулей)/vitest 11/11 зелёные, `docker compose run --rm lint` чисто, `docker compose run --rm e2e` Playwright 2/2. Ручной визуальный QA всех экранов в браузере не выполнялся. |
| 40 | Двусторонний email-канал (P0, Фаза 3, DEC-049) — **0.11.0** | done | Тип канала `email` + миграция `0003`; IMAP-поллер `email_poller.py` (изоляция `imaplib`), beat `poll_email_channels` (дедуп по `Message-ID`, каждые 3 мин), ветки `email` в `send_outgoing`/`normalize_incoming_payload`; фронтенд — тип «Электронная почта» и поля IMAP/SMTP в форме канала. Email переиспользует конвейер сообщений и WS (DEC-019). Разбор письма: HTML-fallback (теги срезаются) + метаданные вложений. Валидация: миграция `0003` без дрейфа, `check` 0 issues, 10 тестов `test_email_channel` (вкл. HTML/вложения) + регрессия каналов OK, ruff чисто, frontend typecheck/build 627 модулей; **e2e `email-channel.spec.ts` — `3 passed` (создание канала через форму в браузере)**; **сквозной SMTP/IMAP round-trip на боевом ящике `hello@prvms.ru` (Beget) из `.env`: SMTP `delivered=True`, IMAP `new=1` с маркером в теле — оба стыка подтверждены.** Остаток (KNOWN_ISSUES #28): инлайн-HTML/содержимое вложений, OAuth Gmail/Yandex, тред-заголовки. |
| 41 | Веб-формы захвата лидов + виджет (P1, Фаза 4, DEC-050) — **0.12.0** | done | Модель `WebForm` (`crm/0007`) + shared-lookup `WebFormLookup` (`tenants/0007`); публичные `webform_submit`/`webform_schema` (honeypot/rate-limit/CORS); сервис `webform_intake` (Contact+Deal+distribute+notify); CRUD `webforms_api`; сущность прав `webforms` (`users/0005`); фронтенд-конструктор `WebFormsView`, пункт меню, код для вставки; статический виджет `frontend/public/widget/crm-webform.js`. Валидация: миграции без дрейфа, `check` 0 issues, `Ran 7 tests … OK` (`test_webforms`+RBAC), ruff чисто, frontend typecheck/build 630 модулей; **e2e `webforms.spec.ts` — `4 passed`**; **живой `POST /api/public/webform/<token>/` → `HTTP 200`, в `company-1` создались контакт+сделка, `submissions_count=1`.** Остаток: встраивание виджета на чужой сайт сквозным результатом не проверено (endpoint подтверждён зондом), капча не добавлена. |
| 42 | Теги и сегменты (P1, Фаза 7, DEC-051) — **0.13.0** | done | Модели `Tag` (M2M к Contact/Deal) и `Segment` (`crm/0008`); CRUD и назначение `tags_api` (`tags/`/`segments/`/`contacts|deals/{id}/tags/`); фильтр `tag_id` в списках контактов/сделок; фронтенд — страница «Теги» (создание/список/удаление с цветом), пункт меню. Гейт правом `contacts`/`deals` (без новой RBAC-сущности). Валидация: миграция `crm/0008` без дрейфа, `check` 0 issues, `Ran 4 tests … OK` (`test_tags`), ruff чисто, frontend typecheck/build 633 модуля; **e2e `tags.spec.ts` — `5 passed`**. Остаток: UI назначения тегов в карточках и UI сегментов (бэкенд готов) — KNOWN_ISSUES #30. |
| 43 | Конструктор автоматизаций + SLA (P1, Фаза 5, DEC-052) — **0.14.0** | done | Модели `AutomationRule`/`AutomationRunLog` (`crm/0009`); общий `execute_action` (рефактор `process_stage_change` без слома); событийные триггеры `new_deal`/`stage_changed` из `create_deal`/`move_deal`; beat `evaluate_time_rules` (`no_activity`, 15 мин) с идемпотентностью через `AutomationRunLog`; CRUD `automation_api` (owner/admin); фронтенд — конструктор `AutomationView`, пункт меню. Валидация: миграция `crm/0009` без дрейфа, `check` 0 issues, `Ran 5 tests … OK` (`test_automation` + регрессия `test_auto_actions`), ruff чисто, frontend typecheck/build 636 модулей; **e2e `automation.spec.ts` — `6 passed`**. Остаток (KNOWN_ISSUES #31): триггер `sla_breach`, UI действий `change_stage/assign/create_document`. |
| 47 | Транскрипция и резюме звонков (P0, Фаза 2, DEC-056) — **0.18.0** | done | ASR — Deepgram (`deepgram_client.py`, контракт сверён живым вызовом: `POST /v1/listen`, `Authorization: Token`, raw-bytes `audio/mpeg`, `results.channels[0].alternatives[0].transcript`); резюме — существующий Hermes (`summarize_call_text`); модель `CallTranscript` (`telephony/0004`); Celery `transcribe_call_record`→`summarize_call`, запуск из `download_call_record`, резюме как `Activity(note)` в таймлайне сделки; фича `ai_call_intelligence` (`billing/0009`) гейтует фон и endpoint-ы `POST /telephony/calls/{id}/transcribe/`+`GET …/transcript/`; фронтенд — колонка «AI-резюме» с кнопкой «Распознать» в журнале. Валидация: миграции без дрейфа, `check` 0 issues, ruff чисто, **backend 8/8** (`test_call_intelligence`) + регрессия `telephony/billing` 27/27, typecheck/build EXIT=0, vitest 11/11; **живой [граница] зонд Deepgram через сам клиент: confidence 0.998, real request_id, raw-bytes путь.** Сквозной результат (реальная запись→резюме в ЛК) — только после деплоя на прод, KNOWN_ISSUES #36. |
| 48 | Hermes без onboarding: OpenCode config-as-code (DEC-057) | done | `config/hermes/config.yaml` задаёт `opencode-go/qwen3.6-plus`; `hermes-init` готовит writable `hermes_profiles` volume и CRM skills; `hermes` добавлен в prod compose; env-контракт выровнен на `OPENCODE_API_KEY`; Django отправляет `model=hermes-agent`. Валидация: compose config OK, `check` 0 issues, ruff чисто, 7/7 `ai_assistant` tests, live Hermes `/health`+`/v1/models` 200 и `chat/completions` → `ok`. |
| 46 | Планы продаж + аналитика воронки (P2, Фаза 10, DEC-055) — **0.17.0** | done | Модель `SalesTarget` (`crm/0012`, план по менеджеру на месяц: `target_amount`+`target_count`, upsert); `move_deal` теперь выставляет/сбрасывает `closed_at` при won/lost/open (инвариант периодной аналитики); роутер `analytics_api` — `funnel` (стадии+win-rate), `loss-reasons`, `forecast`, CRUD `targets` (owner/admin), `target-progress` (план vs факт); фронтенд — `StatsView` расширен секциями воронки/прогноза/причин потерь, новый `SalesTargetsView` (план+прогресс), пункты меню «Аналитика»/«Планы продаж». Воронка — на текущем распределении (история без `stage_id`). Валидация: миграция `crm/0012` без дрейфа, `check` 0 issues, ruff чисто, **backend 7/7** (`test_analytics_targets`) + регрессия `crm` 54/54, typecheck EXIT=0, build EXIT=0, vitest 11/11; **e2e `sales-analytics.spec.ts` — `1 passed` (живой рендер воронки/прогноза + страница планов)**; **живой зонд на боевом тенанте [граница]: target-progress факт 700/план 1000 = 70%, count 1/2.** Остаток — KNOWN_ISSUES #34. |
| 45 | Календарь + напоминания + повторяемость (P2, Фаза 9, DEC-054) — **0.16.0** | done | Поля `recurrence_rule`(RRULE)/`remind_at`/`reminder_sent_at` у `Activity` (`crm/0011` + индекс `status,remind_at`); сервис `services/recurrence.py` (`next_occurrence` через `python-dateutil`, контракт сверён); спавн следующего экземпляра при закрытии задачи в `patch_activity`; событие `task_reminder` + помощник `notify_user` (адресно ответственному); beat `send_task_reminders` (5 мин, идемпотентность через `reminder_sent_at`); миграции `notifications/0003` (alter choices) и `notifications/0004` (бэкафилл настроек для существующих тенантов — дефект пойман живым зондом); endpoint `GET /crm/activities/calendar/`; фронтенд — `CalendarView` на **FullCalendar 6.1.21** (vue-cal отклонён как EOL/нетипизированный), диалог задачи с пресетами RRULE+сырым правилом и смещением напоминания, пункт меню «Календарь». Валидация: миграции без дрейфа, `check` 0 issues, ruff чисто, **backend 9/9** (`test_calendar_reminders`) + регрессия `crm/notifications` 50/50, typecheck EXIT=0, build EXIT=0, vitest 11/11; **e2e `calendar.spec.ts` — `1 passed` (создание задачи из календаря, событие в сетке)**; **живой зонд через реальный Celery-worker [сквозь]: напоминание → in-app `Notification` ответственному (delta=1 после бэкафилла), `next_occurrence` +7 дней.** Остаток — KNOWN_ISSUES #33. |
| 44 | Импорт/экспорт + дедуп/слияние (P1, Фаза 6, DEC-053) — **0.15.0** | done | Модель `ImportJob` (`crm/0010`); сервис `services/import_export.py` (парсинг CSV/XLSX через `openpyxl==3.1.5` + CSV-экспорт с BOM); сервис `services/merge.py` (поиск дублей и транзакционное слияние контактов/компаний с переносом сделок/активностей и аудитом); Celery `import_records` (батчи 50, дедуп phone/email и ИНН/название, построчный `errors[]`); роутер `import_api.py` (`import/preview|run`, `import/jobs/{id}`, `export/{entity}`, `duplicates/{entity}`, `merge/{entity}`); права переиспользуют `contacts`/`companies` (view/create/delete + scope); фронтенд — единый `DataToolsView` (мастер импорта/экспорт/дубли), пункт меню «Импорт/экспорт». Охват — контакты и компании (сделки отложены). Валидация: миграция `crm/0010` без дрейфа, `check` 0 issues, ruff чисто, **backend 12/12** (`test_import_merge`) + регрессия `crm/audit/users` 69/69, typecheck EXIT=0, build EXIT=0, vitest 11/11; **e2e `data-tools.spec.ts` — `1 passed` (живой экспорт CSV download + поиск дублей)**; **живой цикл импорта через реальный Celery-worker [сквозь]: `.delay`→`done`, created=1/updated=1 (дедуп по телефону), ошибка пустой строки, `custom_fields` записан.** Остаток — KNOWN_ISSUES #32. |
| 47 | Полная актуализация руководства пользователя (`docs/user-guide`) — **0.17.1** | done | Статьи перенумерованы под порядок живого меню `AppMenu.vue` (3 группы); «Встроенный CRM» разбит на Контакты/Компании/Сделки; переписаны телефония (FreeSWITCH→МТС Exolve) и распределение (вкладка «Команды»); обновлены дашборд/аналитика/документы/каналы/команда/права/уведомления/настройки/подписка/регистрация/воронки; добавлены 9 новых статей (Товары, Веб-формы, Теги, Автоматизации, Календарь, Планы продаж, Импорт/экспорт, Чаты, AI-ассистент); `README` переписан. Итог: `01`…`29` + `README` + `vk-channel`, удалены 16 старых файлов. Код не менялся. Валидация: `down/up --build`, `check` 0 issues, frontend typecheck EXIT=0 + build OK (glob с новым набором), маркеры новых статей в бандле, SPA `/app/help` 200 + `/healthz` 200. Браузерный QA рендера не выполнялся (нет браузера). Найдены KNOWN_ISSUES #35 (сирота `/app/pipelines`, мёртвые `SidebarNav.vue`/`TopBar.vue`). |

## Завершённые задачи
| # | Задача | Дата | Заметки |
|---|--------|------|---------|
| 11 | Подписка и тарифы hardening (чеклист Н, пп. 62–64) | 2026-04-18 | Для `trial_expired` сохранён доступ к странице подписки и billing-операциям; usage лимитов унифицирован в одном сервисе и показывает `pipelines`; managers считают только active+joined membership ролей owner/admin/manager; feature-gating для внешних CRM усилен для операций по существующим connection в API и lock-состояний в UI; список планов унифицирован по DTO между регистрацией и ЛК |
| 12 | DevOps: bootstrap скрипт первичной настройки сервера | 2026-04-29 | Единый скрипт готовит сервер с нуля под Docker-инфраструктуру (Traefik + Portainer), гарантирует наличие общей сети `proxy` и генерирует управляющие скрипты `/opt/scripts/*` |
| 10 | Этап Ж: внешние CRM (webhook + marketplace UX) | 2026-04-16 | Реализованы one-click marketplace install для amoCRM/Битрикс24 с tenant-bound state, двухконтурный UI setup (marketplace vs webhook), автонастройка после OAuth callback (default webhook + sync + health), единый endpoint проверки интеграции (connection + webhook), пользовательский журнал ошибок и статусы («Работает / Требуется авторизация / Ошибка webhook / Недостаточно прав»), auto-reconnect токенов и scope-валидация |
| 9 | RBAC для CRM (чеклист Р) | 2026-04-16 | Добавлена модель `RolePermission` в public schema, дефолтные профили прав owner/admin/manager/viewer, API управления матрицей прав, scope-фильтрация `all/team/own` в CRM endpoints (deals/contacts/companies), UI «Команда → Права ролей», скрытие/блокировка CRUD в CRM UI, аудит изменений роли/прав |
| 8 | Hotfix инвайтов: онбординг + принятие приглашений после онбординга | 2026-04-16 | Исправлен шаг онбординга “Менеджеры”: теперь создаёт pending membership + invite link. Исправлена классификация legacy-пользователей с `password=''` — больше не требуется “неизвестный старый пароль” при принятии приглашения |
| 7 | Multi-org контур: безопасные приглашения + переключение организации | 2026-04-16 | Добавлены `/api/auth/organizations` и `/api/auth/switch-tenant`; ужесточён flow принятия приглашений для существующих аккаунтов (подтверждение паролем), `tenant_slug` по умолчанию выбирается только из активных joined membership, в TopBar добавлен переключатель организаций |
| 6 | Dev-infra: параметризация host-портов Docker | 2026-04-14 | `docker-compose` переведен на `*_HOST_PORT` переменные (`web/frontend/db/redis/freeswitch`), добавлен единый блок портов в `.env/.env.example` для параллельного запуска нескольких инстансов |
| 5 | Dev-tooling: management-команда тестовых пользователей | 2026-04-14 | `create_test_users` без аргументов делает bootstrap seed (3 tenant-а `simple/basic/crm`, 6 пользователей owner/manager, platform admin); в tenant-режиме создаёт `owner/admin/manager/viewer`; смена паролей существующих аккаунтов выполняется только с `--reset-password`; отчёт команды явно показывает `password:set/password:kept` и username; `platform_admin` автоматически получает membership `admin` в `org-crm`; покрыто 5 автотестами команды + auth-регрессии на login по username/email |
| 4 | Публичный входной контур продукта | 2026-04-14 | Добавлены landing + регистрация + логин в SPA, ЛК перенесён на `/app`, backend `:8000` перенаправляет на frontend |
| 3 | Базовый автотест-контур backend + frontend | 2026-04-14 | Добавлены и стабилизированы 31 backend тест (tenant/auth/contracts/integrations/telephony/crm/audit/notifications/distribution/channels) и 2 frontend Vitest теста (HTTP tenant headers) |
| 2 | Полная реализация backend+frontend спецификации v1 | 2026-04-13 | Поднят end-to-end Docker стек, добавлены SPA и production frontend образ, закрыты критичные runtime-блокеры |
| 1 | Каркас: tenants + billing + users + auth | 2026-04-13 | Добавлены миграции, исправлен Docker bootstrap, `manage.py check` и health-check проходят |
